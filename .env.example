# 评论功能（Valine / LeanCloud）凭据。复制为 .env 并填写自己的值：
#   cp .env.example .env
# 构建/开发前导出到环境变量（.env 已被 .gitignore 忽略，不会提交）：
#   set -a && source .env && set +a && npm run dev
# 未设置时评论功能会自动关闭，其余站点功能不受影响。
VALINE_APP_ID=
VALINE_APP_KEY=
